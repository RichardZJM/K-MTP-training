mlip-type          mtp
mtp-filename    $mtp
calculate-efs   TRUE
select             TRUE              #selection mode is activated
                   select:threshold                   $select        #the configurations with th grades greater than 2.1 will be added to the preselected se
                   select:threshold-break             $break       #molecular dynamics will be ade of current configuration from scratch is greater than 10
                   select:save-selected               preselected.cfg #preselected configurations will be saved to this file
                   select:load-state                  $als #active learning state is loaded from this file
